name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment: [staging, production, dev] # List your environments here
    env:
      # You can define environment-specific variables here
      BASE_URL: ${{ matrix.environment == 'staging' && 'https://automationexercise.com' || (matrix.environment == 'production' && 'https://automationexercise.com' || 'https://automationexercise.com') }}
      # Optionally add other environment-specific variables like:
      # API_KEY: ${{ secrets.API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright Tests
        run: npx playwright test

      - name: Upload Playwright report as artifact
        if: always()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: playwright-report
          path: playwright-report
